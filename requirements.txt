six >= 1.11.0
rootpath >= 0.1.1
inspecta >= 0.1.0

# # setuptools never belongs in a requirements.txt
# setuptools >= 40.8.0

# # TODO: move this to a requirements-dev.txt
# colour-runner >= 0.0.5
# deepdiff >= 3.3.0
# tox >= 3.0.0
# coverage >= 4.5.2
# codecov >= 2.0.15
